---
import '../global.css';
const tabs = [
  { name: 'Cinematic', content: 'Capturing dynamic aerial shots for movies, TV shows, commercials, and music videos.' },
  { name: 'RealEstate', content: 'Providing immersive virtual tours and showcasing properties with stunning aerial views.' },
  { name: 'SpecialEvents', content: 'Recording weddings, corporate events, sports events, and more from unique perspectives.' },
  { name: 'Inspections', content: 'Conducting detailed inspections of infrastructure, buildings, and agricultural fields.' },
  { name: 'Inspections', content: 'Conducting detailed inspections of infrastructure, buildings, and agricultural fields.' }

];
---


<head>
  <meta charset="UTF-8">
  <head/>
  
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');

    
@media only screen and (max-width:1000px){
  .tab {
      overflow: hidden;
      border-bottom: 1px solid #ccc;
    }
    .tab button {
      /* background-color: inherit; */
      background-color: #31363f;
      color: #ddd;

width:50%;
      border: none;
      outline: none;
      cursor: pointer;
      margin:0px;
      border-radius: 0px;
      padding: 20px;
      transition: 0.3s;
      font-size: 1.2em;
      font-style: normal;
      font-weight: 200;
    }
    .tab button:hover {
      background-color: #ddd;
      color: #31363f;
    }
    .tab button.active {
      background-color: #76ABAE;
      color: #ddd;
    }
    .tabcontent {
      display: none;
      /* padding: 16px; */
      border-top: none;
    }
    .tabcontent.active {
      display: block;
      
    }
}
@media only screen and (min-width:1000px){
  @import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');


    .tab {
      overflow: hidden;
      border-bottom: 1px solid #ccc;
    }
    .tab button {
      /* background-color: inherit; */
      background-color: #31363f;
      color: #ddd;


      border: none;
      outline: none;
      cursor: pointer;
      margin:0px;
      border-radius: 0px;
      padding: 14px 16px;
      transition: 0.3s;
      font-size: 17px;
      /* font-style: normal; */
      font-weight: 100;
    }
    .tab button:hover {
      background-color: #ddd;
      color: #31363f;
    }
    .tab button.active {
      background-color: #76ABAE;
      color: #ddd;
    }
    .tabcontent {
      display: none;
      /* padding: 16px; */
      border-top: none;
    }
    .tabcontent.active {
      display: block;
    }
  }
  .tablinks{
    font-weight: 100;
  }
  
  </style>
<main style="z-index: 1;">
  <!-- <h2>Our Services</h2> -->
  <p>Click on the tabs to learn more about each service.</p>

  <div class="tab">
    {tabs.map(tab => (
      <button class="tablinks" id={`btn-${tab.name}`} data-tab={tab.name}>
        {tab.name.replace(/([A-Z])/g, ' $1').trim()}
      </button>
    ))}
  </div>

  {tabs.map(tab => (
    <div id={tab.name} class="tabcontent">
      <h3>{tab.name.replace(/([A-Z])/g, ' $1').trim()}</h3>
      <p>{tab.content}</p>
    </div>
  ))}

 
</main>

<script>
  document.addEventListener('DOMContentLoaded', (event) => {
    function openTab(tabName) {
      var i, tabcontent, tablinks;
      tabcontent = document.getElementsByClassName("tabcontent");
      for (i = 0; i < tabcontent.length; i++) {
        tabcontent[i].classList.remove('active');
      }
      tablinks = document.getElementsByClassName("tablinks");
      for (i = 0; i < tablinks.length; i++) {
        tablinks[i].classList.remove('active');
      }
      document.getElementById(tabName).classList.add('active');
      document.querySelector(`button[data-tab="${tabName}"]`).classList.add('active');
    }

    // Set default active tab
    openTab('Cinematic');

    document.querySelectorAll('.tab button').forEach(button => {
      button.addEventListener('click', function () {
        openTab(this.getAttribute('data-tab'));
      });
    });
  });
</script>